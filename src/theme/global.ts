import { colors } from './colors'

export const global = {
  styles: {
    global: {
      'html, body': {
        bg: colors.background,
        h: 'full',
      },
      '#__next': {
        h: 'full',
      },
    },
  },
}
